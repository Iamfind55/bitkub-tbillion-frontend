import Maincontent from "@/utils/maincontent";
import React from "react";
import TableType from "./tabletype";

function page() {
  return (
    <Maincontent>
      <TableType />
    </Maincontent>
  );
}

export default page;
